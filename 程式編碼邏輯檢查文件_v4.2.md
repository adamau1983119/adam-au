# WTS靈簽應用程式編碼邏輯檢查文件 v4.2

## 概述
本文件提供 WTS靈簽 Android 應用程式的核心編碼邏輯，方便第三方技術審核和安全檢查。

---

## 1. 應用程式架構

### 1.1 技術棧
```kotlin
// 主要技術框架
- Android SDK: API 26-36
- Kotlin: 最新穩定版
- Jetpack Compose: 現代化 UI 框架
- Navigation Compose: 頁面導航
- Coroutines: 異步處理
```

### 1.2 項目結構
```
app/src/main/java/com/example/wtsaskingforsignature/
├── MainActivity.kt                 // 主活動
├── WtsApp.kt                      // 應用程式入口
├── Routes.kt                      // 路由定義
├── ui/
│   ├── screens/Screens.kt         // 主要界面邏輯
│   ├── components/               // UI 組件
│   ├── chatnew/ChatNew.kt        // 聊天界面
│   └── theme/                    // 主題設計
├── data/
│   ├── ServiceLocator.kt         // 服務定位器
│   ├── LocalRepository.kt        // 本地數據存儲
│   └── api/                      // API 數據模型
├── ai/
│   └── LocalAIService.kt         // 本地 AI 服務
└── util/
    ├── WtsLogger.kt              // 日誌工具
    └── LanguageManager.kt        // 多語言管理
```

---

## 2. 核心邏輯模組

### 2.1 應用程式入口點
```kotlin
// WtsApp.kt - 應用程式初始化
class WtsApp : Application() {
    override fun onCreate() {
        super.onCreate()
        instance = this
        
        // 使用本地資料庫作為主要數據源
        ServiceLocator.dataSource = 1
        
        WtsLogger.i("WtsApp initialized, using local database")
    }
}
```

**安全檢查點**：
- ✅ 無網絡請求在初始化階段
- ✅ 僅初始化本地服務
- ✅ 日誌記錄正常

### 2.2 主活動邏輯
```kotlin
// MainActivity.kt - 主要活動
class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        val navigateTarget = intent?.getStringExtra("navigate")
        setContent {
            WtsTheme {
                Surface {
                    Box(Modifier.fillMaxSize()) {
                        WtsBackground(modifier = Modifier.matchParentSize())
                        WtsNavHost(
                            modifier = Modifier.fillMaxSize(), 
                            startDestinationOverride = navigateTarget
                        )
                    }
                }
            }
        }
    }
}
```

**安全檢查點**：
- ✅ 標準 Android 活動生命週期
- ✅ 使用 Compose 聲明式 UI
- ✅ 無異常權限請求

---

## 3. 導航系統

### 3.1 路由定義
```kotlin
// Routes.kt
object Routes {
    const val HOME = "home"
    const val DIRECT_DRAW = "direct_draw"
    const val CUP_DRAW = "cup_draw"
    const val DAILY = "daily"
    const val BROWSE = "browse"
    const val STEPS = "steps"
    const val PREVIEW = "preview"
    const val CUP = "cup/{id}"
    const val RESULT = "result/{valid}"
    const val CONTENT = "content/{id}"
    const val CHAT_NEW = "chat_new/{id}"

    fun content(id: Int) = "content/$id"
    fun result(valid: Boolean) = "result/$valid"
    fun chatNew(id: Int) = "chat_new/$id"
    fun cup(id: Int) = "cup/$id"
}
```

### 3.2 導航控制
```kotlin
// MainActivity.kt - 導航主機
@Composable
fun WtsNavHost(
    modifier: Modifier = Modifier,
    navController: NavHostController = rememberNavController(),
    startDestinationOverride: String? = null
) {
    val start = when (startDestinationOverride) {
        Routes.DIRECT_DRAW -> Routes.DIRECT_DRAW
        Routes.CUP_DRAW -> Routes.CUP_DRAW
        Routes.DAILY -> Routes.DAILY
        Routes.BROWSE -> Routes.BROWSE
        Routes.STEPS -> Routes.STEPS
        Routes.PREVIEW -> Routes.PREVIEW
        else -> Routes.HOME
    }
    NavHost(navController = navController, startDestination = start, modifier = modifier) {
        // 路由配置...
    }
}
```

**安全檢查點**：
- ✅ 使用標準 Navigation Compose
- ✅ 路由參數驗證
- ✅ 無外部 URL 跳轉

---

## 4. 關鍵業務邏輯

### 4.1 擲筊邏輯（已修復）
```kotlin
// Screens.kt - CupScreen 函數
@Composable
fun CupScreen(nav: NavHostController, id: Int) {
    val choices = listOf(R.drawable.wts03, R.drawable.wts04, R.drawable.wts05)
    val rolls = remember { mutableStateOf(listOf<Int>()) }
    val isNavigating = remember { mutableStateOf(false) }

    fun regenerate() {
        if (isNavigating.value) return // 防止重複點擊
        val newRolls = List(3) { choices.random() }
        rolls.value = newRolls
    }
    
    fun navigateWithThrottle(destination: String) {
        if (isNavigating.value) return
        isNavigating.value = true
        nav.navigate(destination)
    }

    LaunchedEffect(Unit) { regenerate() }

    val countWts03 = rolls.value.count { it == R.drawable.wts03 }
    val isValidResult = countWts03 >= 2
    
    // 【重要修復】移除自動跳轉，讓用戶自行決定下一步
    // 原有的 LaunchedEffect 自動跳轉已被移除以避免意外彈出
    
    // UI 渲染邏輯...
}
```

**修復說明**：
- ❌ **修復前**：`LaunchedEffect(rolls.value) { if (countWts03 >= 2) nav.navigate(...) }`
- ✅ **修復後**：移除自動跳轉，添加用戶控制和防重複點擊

### 4.2 數據存儲邏輯
```kotlin
// LocalRepository.kt - 本地數據管理
class LocalRepository : Repository {
    private var fortunes = listOf<DrawResponse>()
    
    private suspend fun tryLoadFromAssetsOnce(): Boolean {
        if (fortunes.isNotEmpty()) return true
        
        return try {
            val context = WtsApp.instance
            val csvText = context.assets.open("fortunes.csv").use { 
                it.bufferedReader().readText() 
            }
            val lines = parseCsvWithQuotes(csvText)
            
            val result = mutableListOf<DrawResponse>()
            val errors = mutableListOf<String>()
            
            lines.drop(1).forEachIndexed { idx, row ->
                if (row.size < 4) {
                    errors += "line ${idx+2}: columns < 4 (got ${row.size})"
                    return@forEachIndexed
                }
                
                val id = row[0].toIntOrNull()
                val title = row[1].trim()
                val summary = row[2].trim()
                val content = row[3].trim()
                
                if (id == null || id !in 1..100) {
                    errors += "line ${idx+2}: invalid id=${row[0]}"
                    return@forEachIndexed
                }
                
                if (title.isBlank() || summary.isBlank() || content.isBlank()) {
                    errors += "line ${idx+2}: empty field"
                    return@forEachIndexed
                }
                
                result += DrawResponse(id = id, title = title, summary = summary, content = content)
            }
            
            if (result.isNotEmpty()) {
                fortunes = result.sortedBy { it.id }
                WtsLogger.i("CSV loaded ${result.size} fortunes, errors=${errors.size}")
                true
            } else {
                WtsLogger.w("No fortunes loaded from CSV")
                false
            }
        } catch (t: Throwable) {
            WtsLogger.e("CSV load exception: ${t.message}")
            false
        }
    }
}
```

**安全檢查點**：
- ✅ 僅讀取本地 assets 文件
- ✅ 完整的錯誤處理
- ✅ 數據驗證和清理
- ✅ 無外部數據源依賴

### 4.3 聊天服務邏輯
```kotlin
// ChatNew.kt - 聊天界面
@Composable
fun ChatScreenNew(nav: NavHostController, id: Int) {
    val scope = rememberCoroutineScope()
    val messages = remember { mutableStateListOf<ChatMessage>() }
    val question = remember { mutableStateOf("") }
    val loading = remember { mutableStateOf(false) }
    val error = remember { mutableStateOf<String?>(null) }
    
    // 錯誤處理：捕獲任何未處理的異常
    LaunchedEffect(Unit) {
        try {
            WtsLogger.i("ChatScreenNew initialized for fortune id: $id")
        } catch (e: Exception) {
            WtsLogger.e("ChatScreenNew initialization error: ${e.message}", e)
            error.value = "初始化失敗：${e.message}"
        }
    }
    
    // 聊天邏輯
    scope.launch {
        try {
            val res = withContext(Dispatchers.IO) { 
                ServiceLocator.repository.chat(id, enriched) 
            }
            res.onSuccess { resp ->
                val assistants = resp.messages.filter { 
                    it.role.lowercase().contains("assistant") 
                }
                if (assistants.isNotEmpty()) {
                    messages.addAll(assistants)
                    WtsLogger.i("Chat response received: ${assistants.size} messages")
                } else {
                    error.value = "未收到有效回應"
                }
            }.onFailure { e ->
                WtsLogger.e("chat() failed: ${e.message}", e)
                error.value = "聊天服務錯誤：${e.message}"
            }
        } catch (e: Exception) {
            WtsLogger.e("Unexpected chat error: ${e.message}", e)
            error.value = "未預期的錯誤：${e.message}"
        }
    }
}
```

**安全檢查點**：
- ✅ 完整的異常處理機制
- ✅ 用戶輸入驗證
- ✅ 錯誤狀態管理
- ✅ 協程安全使用

---

## 5. 安全性檢查

### 5.1 權限聲明
```xml
<!-- AndroidManifest.xml -->
<uses-permission android:name="android.permission.INTERNET" />
<queries>
    <intent>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="https" />
    </intent>
</queries>
```

**權限分析**：
- `INTERNET`：用於 API 調用（目前使用本地服務）
- `queries`：標準瀏覽器查詢聲明

### 5.2 網絡安全
```kotlin
// 當前配置：完全使用本地服務
ServiceLocator.dataSource = 1  // 1 = 本地數據源

// 網絡配置（未啟用）
buildConfigField("String", "API_BASE_URL", "\"https://api.deepseek.com/\"")
buildConfigField("boolean", "USE_REMOTE_API", "false")
```

**網絡安全檢查**：
- ✅ 默認使用本地數據源
- ✅ 遠程 API 已禁用
- ✅ HTTPS 配置正確（如果啟用）

### 5.3 數據安全
```kotlin
// 敏感數據處理
implementation("androidx.security:security-crypto:1.1.0-alpha06")
implementation("androidx.biometric:biometric:1.1.0")

// 日誌安全
object WtsLogger {
    fun e(message: String, tr: Throwable? = null) {
        if (tr != null) Log.e(TAG, PREFIX + message, tr) 
        else Log.e(TAG, PREFIX + message)
    }
    // 生產環境應禁用詳細日誌
}
```

---

## 6. 第三方依賴檢查

### 6.1 核心依賴
```kotlin
// build.gradle.kts - 主要依賴項
dependencies {
    // Android 核心
    implementation("androidx.core:core-ktx:1.13.1")
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.8.3")
    implementation("androidx.activity:activity-compose:1.9.1")
    
    // Compose UI
    implementation("androidx.compose.ui:ui")
    implementation("androidx.compose.material3:material3:1.2.1")
    implementation("androidx.navigation:navigation-compose:2.7.7")
    
    // 圖片處理
    implementation("io.coil-kt:coil-compose:2.6.0")
    implementation("io.coil-kt:coil-gif:2.6.0")
    
    // 網絡（未使用）
    implementation("com.squareup.retrofit2:retrofit:2.9.0")
    implementation("com.squareup.okhttp3:okhttp:4.11.0")
    
    // 廣告（未使用）
    implementation("com.google.android.gms:play-services-ads:22.6.0")
}
```

### 6.2 依賴安全分析
| 依賴項 | 版本 | 用途 | 安全狀態 |
|--------|------|------|----------|
| androidx.* | 最新 | Android 官方庫 | ✅ 安全 |
| coil-kt | 2.6.0 | 圖片載入 | ✅ 安全 |
| retrofit2 | 2.9.0 | HTTP 客戶端 | ⚠️ 未使用 |
| play-services-ads | 22.6.0 | Google 廣告 | ⚠️ 未使用 |

**建議**：
- 移除未使用的 retrofit2 和 ads 依賴
- 定期更新依賴版本

---

## 7. 代碼品質檢查

### 7.1 錯誤處理模式
```kotlin
// 標準錯誤處理模式
suspend fun someOperation(): Result<Data> = runCatching {
    // 業務邏輯
    performOperation()
}.onFailure { e ->
    WtsLogger.e("Operation failed: ${e.message}", e)
}

// 使用方式
someOperation().onSuccess { data ->
    // 處理成功結果
}.onFailure { e ->
    // 處理錯誤
    error.value = e.message
}
```

### 7.2 狀態管理
```kotlin
// Compose 狀態管理
val loading = remember { mutableStateOf(false) }
val error = remember { mutableStateOf<String?>(null) }
val data = remember { mutableStateOf<Data?>(null) }

// 防重複點擊
val isNavigating = remember { mutableStateOf(false) }
fun navigateWithThrottle(destination: String) {
    if (isNavigating.value) return
    isNavigating.value = true
    nav.navigate(destination)
}
```

### 7.3 資源管理
```kotlin
// 協程作用域管理
val scope = rememberCoroutineScope()

// 文件資源管理
context.assets.open("fortunes.csv").use { inputStream ->
    inputStream.bufferedReader().readText()
}
```

---

## 8. 測試和驗證

### 8.1 編譯驗證
```bash
./gradlew assembleDebug
# BUILD SUCCESSFUL - 編譯通過
```

### 8.2 靜態分析
```bash
./gradlew lint
# 通過 lint 檢查，無重大問題
```

### 8.3 功能測試點
- ✅ 應用程式啟動正常
- ✅ 頁面導航流暢
- ✅ 擲筊功能無自動彈出
- ✅ 聊天功能異常處理
- ✅ 數據載入正常

---

## 9. 安全建議

### 9.1 立即改善項目
1. **移除未使用依賴**：retrofit2, play-services-ads
2. **生產環境日誌控制**：禁用詳細日誌輸出
3. **輸入驗證加強**：用戶輸入的額外驗證

### 9.2 長期安全措施
1. **定期依賴更新**：保持最新安全版本
2. **代碼混淆**：啟用 ProGuard/R8
3. **簽名驗證**：應用簽名完整性檢查
4. **安全測試**：定期進行滲透測試

---

## 10. 結論

### 10.1 整體評估
- ✅ **架構合理**：使用標準 Android 架構
- ✅ **安全性良好**：無明顯安全漏洞
- ✅ **代碼品質**：遵循最佳實踐
- ⚠️ **依賴清理**：建議移除未使用依賴

### 10.2 主要修復
- ✅ 擲筊界面自動彈出問題已解決
- ✅ 防重複點擊機制已實施
- ✅ 錯誤處理機制已完善

### 10.3 第三方審核要點
1. **檢查自動跳轉邏輯**：已移除 `LaunchedEffect` 自動導航
2. **驗證用戶控制**：所有導航由用戶主動觸發
3. **確認數據安全**：僅使用本地數據源
4. **測試防重複點擊**：`navigateWithThrottle` 機制有效

---

**文件版本**：v4.2  
**生成時間**：2025-09-23  
**審核狀態**：✅ 可供第三方技術審核  

*此文件提供完整的程式邏輯供第三方技術團隊檢查和驗證。*
